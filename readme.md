# Como Rodar a Aplicação

## Configuração
Antes de iniciar crie um arquivo `.env` e defina a variável `WEATHER_API_KEY`.

## Iniciar a Aplicação
Execute:

```sh
docker-compose up -d
```

## Parar a Aplicação
```sh
docker-compose down
```

